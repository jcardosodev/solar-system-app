export interface User {
  id: string;
  nome: string;
  email: string;
  senha: string;
}

export interface TripData {
  name: string;
  age: number;
  companion: string;
  departureDate: string;
  duration: string;
  distance: string;
  price: number;
}