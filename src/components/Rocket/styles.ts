import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  rocketContainer: {
    position: 'absolute',
    top: 0,
    left: 0,
    width: '100%',
    height: '100%',
    zIndex: 10,
  },
  rocket: {
    position: 'absolute',
  },
});